[project]
name = "end-to-end-testing"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = [
    "selenium>=4.0.0",
    "webdriver-manager>=4.0.0",
    "playwright>=1.40.0",
    "pytest>=7.0",
    "pytest-selenium",
    "pytest-playwright",
]

[tool.pytest.ini_options]
pythonpath = ["."]
